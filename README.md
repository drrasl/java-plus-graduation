# Explore With Me — микросервисная архитектура

Проект представляет собой переработанную версию монолитного приложения **"Explore With Me"**, 
разбитую на микросервисы для повышения масштабируемости, гибкости и удобства поддержки. Каждый микросервис отвечает 
за определённую бизнес-область и взаимодействует с другими через внутренние REST API.

Приложение ExploreWithMe (англ. «исследуй со мной») позволит пользователям делиться информацией об интересных событиях 
и находить компанию для участия в них. Также пользователь будет получать подборки рекомендуемых событий на основе истории 
его взаимодействий.  

Пример (фронтэнд отсутствует):
![afisha.png](afisha.png)
---

## 🏗️ Архитектура

### Микросервисы

- **`event-service`** — управление событиями (создание, публикация, поиск и т.д.)
- **`user-service`** — управление пользователями
- **`request-service`** — обработка заявок на участие в событиях
- **`comment-service`** — работа с комментариями к событиям
- **`stats-service`** — сбор и предоставление статистики посещений

### Инфраструктурные сервисы

- **`config-server`** — централизованное хранение конфигураций всех микросервисов
- **`discovery-server` (Eureka)** — сервис обнаружения и регистрации микросервисов
- **`gateway-server`** — единая точка входа для внешних клиентов, маршрутизация запросов

Каждый микросервис имеет собственную базу данных и не зависит напрямую от других на уровне данных. 
Взаимодействие происходит исключительно через HTTP-запросы.

### Микросервис `stats-service`

Переработан из сервера хранения статистики в рекомендательный сервис (на основе платформы для обработки потоков данных `Apache Kafka`. 
Она обеспечит высокую производительность и надёжность, а также позволит обрабатывать данные в реальном времени), 
состоящий из следующих микросервисов с определенными задачами:
- **`collector`** — Принимать сообщения о действиях пользователей, используя gRPC. 
Записывать полученные данные в Kafka-топик stats.user-actions.v1 для дальнейшей обработки.
- **`aggregator`** — Читать данные из топика stats.user-actions.v1 и выполнять следующие вычисления:
  рассчитывать сходство мероприятий и записывать результаты в топик stats.events-similarity.v1. Расчет на основе 
косинусного сходства с использованием весов действий.
- **`analyzer`** —   
1) Читать данные из топиков:  
    a. stats.user-actions.v1 — для хранения информации о последней оценке (максимальном весе действия) пользователей мероприятий;  
    b. stats.events-similarity.v1 — для хранения информации о сходстве мероприятий.  
2) Обновлять базу данных на основе поступающих данных.
3) Обрабатывать запросы от других сервисов по gRPC и предоставлять:  
    a. список рекомендуемых мероприятий для конкретного пользователя на основе сходства мероприятий;  
    b. список мероприятий, которые похожи на указанное и с которыми пользователь ещё не взаимодействовал.

---

## 🔌 Внутреннее API

Взаимодействие между основными (core) микросервисами осуществляется с использованием **Feign Clients** и реализованных 
внутренних эндпоинтов.

### `comment-service` → `event-service`, `user-service`

```java
// Получение события по ID
@GetMapping("/internal/events/{eventId}")
EventDto getEventById(@PathVariable("eventId") Long eventId);

// Получение пользователя по ID или списку ID
@GetMapping("/admin/users/{id}")
UserDto getUserById(@PathVariable("id") Long id);

@GetMapping("/admin/users")
List<UserDto> getUsers(@RequestParam(name = "ids", required = false) List<Long> ids);
```
### `event-service` → `user-service`, `request-service`

```java
// Получение пользователя
@GetMapping("/admin/users/{id}")
UserDto getUserById(@PathVariable("id") Long id);

// Получение заявок по событию
@GetMapping("/internal/requests")
List<RequestDto> getRequestsByEventId(@RequestParam("eventId") Long eventId);

// Подсчёт подтверждённых заявок
@GetMapping("/internal/requests/count/confirmed")
Integer countConfirmedRequestsByEventId(@RequestParam("eventId") Long eventId);

// Обновление статуса заявок
@PostMapping("/internal/requests/status")
void updateRequestsStatus(@RequestBody RequestStatusUpdateDto updateDto);
```
### `request-service` → `event-service`, `user-service`

```java
// Получение события по ID
@GetMapping("/internal/events/{eventId}")
EventDto getEventById(@PathVariable("eventId") Long eventId);

// Получение пользователя по ID или списку ID
@GetMapping("/admin/users/{id}")
UserDto getUserById(@PathVariable("id") Long id);

@GetMapping("/admin/users")
List<UserDto> getUsers(@RequestParam(name = "ids", required = false) List<Long> ids);
```
> Все внутренние эндпоинты защищены от прямого внешнего доступа и доступны только для других микросервисов через Service Discovery и Feign.

### Для повышения надёжности реализованы fallback-методы в Feign-клиентах

Взаимодействие между сервисом статистики (рекомендательным сервисом) и микросервисами бизнес-логики (core) 
осуществляется через технологию gRPC - клиентами. 
- Когда пользователь просматривает событие по эндпоинту микросервиса `event-service`: `GET /events/{id}` - отправляем информацию о просмотре 
пользователем мероприятия с идентификатором id.
- Лайк выставляется по эндпоинту `PUT /events/{eventId}/like` - отправляет в Collector информацию о том, что пользователь 
лайкнул мероприятие. Идентификатор пользователя передается в HTTP-заголовке X-EWM-USER-ID. 
Пользователь может лайкать только посещённые им мероприятия. В противном случае возвращается ошибка 400 BAD REQUEST.
- При регистрации пользователя на событие в микросервисе `request-service` при обработке запроса к эндпоинту 
`POST /users/{userId}/requests` необходимо отправить информацию о регистрации пользователя на мероприятие.
- По эндпоинту `GET /events/recommendations` возвращаются рекомендации мероприятий для пользователя. 
Идентификатор пользователя передается в HTTP-заголовке X-EWM-USER-ID.

---

## 🌐 Внешнее API

Проект реализует публичный и административный API в соответствии со спецификациями Yandex Practicum:

- **Основной сервис (`event-service`)**:  
  [ewm-main-service-spec.json](https://raw.githubusercontent.com/yandex-praktikum/java-explore-with-me/main/ewm-main-service-spec.json)

- **Сервис статистики (`stats-service`) [УСТАРЕЛО - СЕРВИС СТАТИСТИКИ ПЕРЕРАБОТАН В СЕРВИС РЕКОМЕНДАЦИЙ КАК В ОПИСАНИИ ВЫШЕ]**:  
  [ewm-stats-service-spec.json](https://raw.githubusercontent.com/yandex-praktikum/java-explore-with-me/main/ewm-stats-service-spec.json)

---

## ⚙️ Настройка и запуск

1. Запустите Docker и запустите docker-compose.yml - поднимутся все БД на основе PostgreSQL, а также контейнер с кафкой
с созданными топиками и настройками. Для локального запуска можно применять конфигурацию -test (см. конфигурации в конфиг-сервере).
В таком случае будет использоваться БД типа Н2, не требующая подъема PostgreSQL.
2. Запустите **`discovery-server`** — остальные сервисы будут регистрироваться в нём. Порты будут назначаться автоматически.
2. Запустите **`config-server`**.
3. Запустите остальные микросервисы в любом порядке:
    - `user-service`
    - `request-service`
    - `comment-service`
    - `event-service`
    - `stats-service`
    - `collector`
    - `aggregator`
    - `analyzer`
4. Запустите **`gateway-server`** — он будет принимать все внешние запросы и перенаправлять их к нужным микросервисам.
   Доступен по адресу: http://localhost:8080

> Конфигурации каждого сервиса хранятся в `config-server` и загружаются при старте.
